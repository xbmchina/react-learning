import React from 'react';
import ReactDOM from 'react-dom';

const title = 'My Minimal Rfsdfsdeact Webpack 23232323Babel Setup00086787';

ReactDOM.render(
  <div>{title}</div>,
  document.getElementById('app')
);

module.hot.accept();